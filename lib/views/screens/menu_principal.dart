import 'package:flutter/material.dart';

class MenuPriuncipal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          SizedBox(height: 15, width: 15),
        ],
      ),
    );
  }
}
